services:
  openmeet:
    build: .
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
