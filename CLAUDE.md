# OpenMeet - Project Context for Claude Code

## Overview

OpenMeet is a lightweight, terminal-first audio/video conferencing app. Users create/join rooms from a TUI to talk, share webcam and screen, and send text messages. P2P mesh topology (max 6 participants), WebSocket signaling, in-memory Maps (no database), no authentication (a name of up to 8 cells and a colour, chosen once at first start and kept in settings; both travel with `join-room` so everyone draws `[name]` the same way).

**Platform priorities (decided Sept 2026)**: macOS and Windows first; Linux is paused (keep it compiling, don't invest). **Supported OS releases: Windows 11 and macOS 15 (Sequoia) or later**, on real hardware (`ssh win` for Windows, this Mac for macOS). Older releases run but are labelled untested on the home screen (`lib/platform.ts` `withOsNote` appends it to `features`, from `os.release()`: Darwin ≥ 24, NT 10.0 build ≥ 22000). Windows v1.0 scope is rooms + audio + chat; screen sharing works on Windows too (gdigrab), webcam stays macOS/Linux only for now. **Versioning**: one version number for the `openmeet-terminal` package on every platform (0.4.0 introduced Windows + the engine process); platform maturity is expressed by the support matrix in the README and the label next to the version in the TUI, not by per-platform versions. 1.0 = macOS/Windows feature parity. **Runtime (decided Sept 2026, after measuring)**: the Node client gets the cheap corrections and then the client moves to **Go + pion**; `docs/go-migration-analysis.md` holds the research and the argument, `docs/performance.md` the numbers. The short version: the codec is not the cost and JavaScript is not the cost — a full libwebrtc PeerConnection per peer is, and `@roamhq/wrtc` takes only PCM, so a mesh of six is five encoders for one microphone and no amount of care in our code changes that. pion accepts an RTP packet we already encoded and fans it out to every peer, which is the only way to keep the P2P mesh *and* stop paying for it. Constraints the migration inherits, and they are not negotiable: audio stays client-to-client and never passes through a server, and screen and camera stay on WebRTC for its congestion control.

## Architecture

```
Client A <──WebRTC P2P──> Client B
   ↑                        ↑
   │   WebSocket (signaling) │
   └──────> Server <─────────┘
              │
        In-memory Maps
```

- **Topology**: P2P mesh — each client connects directly to every other client
- **Signaling**: WebSocket for SDP/ICE exchange, chat messages, mute state, and screen share state
- **Media**: WebRTC with stereo Opus audio (48 kHz, 128 kbps per direction by default, RED-protected) and VP8/VP9 video (webcam in the camera's own aspect ratio, height ≤ 720 — a 4K camera goes out as 1280x720; screen share in the screen's own aspect ratio, short side ≤ 1080 and long side ≤ 3840 — an ultrawide goes out as 2580x1080 — capped at 2.5 Mbps and scaled down as the mesh grows)
- **Screen sharing**: Simultaneous webcam + screen share via 3 transceivers per connection

## Tech Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| Runtime | Node.js | >=22 |
| Package Manager | pnpm | latest (workspace monorepo) |
| Server Framework | Express | v5 |
| WebSocket | ws | v8 |
| Storage | In-memory Maps | (no database) |
| IDs | nanoid | v5 |
| Linting/Formatting | Biome | v2.5+ |
| TypeScript | typescript | v6 |
| Terminal TUI | Ink (React 19) | v7 |
| Node WebRTC | @roamhq/wrtc (libwebrtc M106) | v0.10 |
| Audio I/O | audify (RtAudio: CoreAudio / WASAPI) | v1.10 |
| Audio I/O (legacy) | sox (rec/play) | system, macOS/Linux only |
| Video I/O | ffmpeg / ffplay | system, macOS/Linux only |
| Terminal Bundler | esbuild | v0.28 |
| CI/CD | GitHub Actions | — |

## Monorepo Structure

```
openmeet/
├── package.json              # Root workspace scripts
├── pnpm-workspace.yaml       # packages/*
├── .npmrc                    # shamefully-hoist=true
├── biome.json                # Shared Biome config
├── Dockerfile                # Multi-stage build (server only)
├── docker-compose.yml        # Single service, port 3001
├── docs/                     # Architecture docs
├── .github/workflows/        # CI/CD workflows
│   ├── ci.yml                # lint/build/type-check + native-module smoke on ubuntu/macos/windows, website build
│   ├── publish-terminal.yml  # npm publish on terminal-v* tags
│   └── deploy-website.yml    # GitHub Pages deploy of packages/website on push to main
└── packages/
    ├── shared/               # @openmeet/shared - WS message types
    ├── server/               # @openmeet/server - Express + ws + in-memory Maps
    ├── terminal/             # openmeet-terminal - TUI client (npm package)
    ├── go/                   # the Go client (migration target): audio-only spike, see packages/go/README.md
    └── website/              # @openmeet/website - openmeet.manuelvega.dev, static, en + es
```

## Package: website (`packages/website`)

The landing page at **openmeet.manuelvega.dev**, private package, never published to npm. Vite 8 (Rolldown + Oxc) with `@vitejs/plugin-react` 6 (no Babel), React 19, TypeScript 7, SCSS (`sass-embedded`, compiled by Vite; `src/styles/site.scss` pulls one partial per area, `_tokens.scss` holds breakpoints and mixins) with the app's palette as CSS custom properties, self-hosted fonts (`@fontsource-variable` IBM Plex Sans for headlines, JetBrains Mono for everything else). Lint and format are the repo's Biome.

**React is a build-time template.** `pnpm --filter @openmeet/website build` runs the client build (CSS, fonts), the SSR build (`src/entry-server.tsx`) and `prerender.mjs`, which writes one complete HTML document per language — `dist/index.html` (en) and `dist/es/index.html` (es) — plus `404.html` and `sitemap.xml`. It finds the stylesheet and the JS to delete in Vite's own `dist/.vite/manifest.json`, so nothing here parses the bundler's HTML. The published page ships no framework: one stylesheet, two woff2 and the page's single inline script. `src/entry-client.tsx` exists for the dev server only (`pnpm --filter @openmeet/website dev`, `/` and `/es/`). `pnpm --filter @openmeet/website check:dist` asserts what came out, reading the page list from the generated sitemap so a new language is covered without editing it.

**The one script is written once.** `src/lib/copy.ts` is the copy buttons' behaviour as a real, type-checked function; `document.tsx` emits it into every page with `` `(${installCopyHandler})()` `` and the dev server calls it directly, so there is no hand-minified twin to drift. Keep that function self-contained — it is serialized with `toString()`.

**Fonts are latin-only.** `src/styles/_fonts.scss` declares the two faces by hand against the `@fontsource-variable` files. Importing the packages whole shipped eleven woff2 (157 KB never fetched) and eleven `@font-face` rules; English and Spanish need one subset each.

**Content and languages.** `src/content/types.ts` is the shape, `en.ts` and `es.ts` the strings, and `src/content/demo.ts` everything that is the same in every language — the demo room's cast and script, the measured install sizes, the chips, the stack. Components read the words through `useCopy()` (`lib/i18n.tsx`) and the rest straight from `demo.ts`, so the two pages cannot drift into showing different demos. English lives at `/` and is the `x-default`; Spanish at `/es/`. Adding a language is a `Copy` file plus an entry in `LANGS`/`PATHS`: `langFromPath` and the header's language links derive from that table rather than naming `es`.

**SEO.** `src/document.tsx` renders the whole `<head>`: title/description per language, canonical, `hreflang` alternates, Open Graph (`public/og.jpg`, 1200×630 from `docs/screenshot.png`) and Twitter cards, and a JSON-LD graph (`SoftwareApplication` with the version read from `packages/terminal/package.json` at build time, `FAQPage` mirroring the visible FAQ, `WebSite`). `public/robots.txt` allows everything including the AI crawlers by name; `public/llms.txt` is the plain-text summary for answer engines. The performance pane quotes measured figures only — install sizes (78 MB against Discord's 479 MB and Chrome's 1.4 GB) and the engine's flat 65 MB — never a total-memory number, for the reason recorded in `docs/performance.md` under "Footprint, measured for the website".

**Deploy.** `deploy-website.yml` runs on pushes to `main` touching `packages/website/**`, then `upload-pages-artifact` → `deploy-pages`. It and `ci.yml`'s `website` job (pull requests only, so a push to `main` does not build the same thing twice) share `.github/actions/build-website`: filtered install (no native modules), type-check, build, `check:dist`. GitHub Pages must be set to source "GitHub Actions"; `public/CNAME` carries the domain, and DNS needs `CNAME openmeet → manuelvegadev.github.io` (the `manuelvega.dev` zone is on Cloudflare with a wildcard, so the record must be explicit, and DNS-only until GitHub has issued the certificate).

## Package: shared (`packages/shared`)

Single source of truth for WebSocket message types as a discriminated union (`WSMessage`).

**Message types**: `join-room`, `room-joined`, `participant-joined`, `participant-left`, `offer`, `answer`, `ice-candidate`, `mute-state`, `screen-share-state`, `chat-message`, `chat-broadcast`, `error`

**Common interfaces**: `Participant` (id, username, joinedAt, optional `color`), `Room` (id, name, createdAt, participantCount). `join-room` and `ChatMessage` carry the same optional `color`; the server copies it from the join onto every chat broadcast, like `username`, so a client never trusts a message's own claim

## Package: server (`packages/server`)

Express v5 HTTP server + WebSocket signaling + in-memory Maps.

### Key files

| File | Purpose |
|------|---------|
| `src/index.ts` | HTTP server + REST routes |
| `src/config.ts` | Port (3001), max 6 participants |
| `src/room-manager.ts` | In-memory Maps for rooms/participants, room removed when empty |
| `src/signaling.ts` | WebSocket connection management, message routing |
| `src/chat.ts` | Chat message broadcasting |
| `src/types.ts` | `ConnectedClient` (ws, participantId, roomId, username) |

### REST API

- `GET /api/rooms` — list all rooms
- `POST /api/rooms` — create room (body: `{ name }`). Unused by the TUI, which lets `join-room` create the room; a room created here and never joined is never collected, because `removeParticipant` only sweeps on disconnect
- `GET /api/rooms/:id` — get single room

### WebSocket signaling flow

1. Client sends `join-room` → server adds to in-memory map, responds with `room-joined` (includes `yourId` + existing participants), broadcasts `participant-joined` to others
2. Signaling messages (`offer`, `answer`, `ice-candidate`) → forwarded directly to target peer by `toId`
3. `mute-state` and `screen-share-state` → broadcast to all other room members
4. `chat-message` → broadcast as `chat-broadcast` to all room members
5. On disconnect → broadcast `participant-left`, remove from map. If room is empty, remove room.

### Important server details

- **Room limit**: 6 participants max, enforced on `join-room`
- **Keepalive**: Server pings all WebSocket clients every 25s to survive reverse proxies
- **No static files**: The server only exposes the REST API and `/ws`. There is no web client.

## WebRTC Implementation Details

### Critical patterns (do NOT change without understanding)

1. **3 transceivers per connection**: Created upfront in the offerer path — index 0 (audio), index 1 (webcam video), index 2 (screen video). This avoids dynamic m-line additions mid-call and ensures both sides have identical transceiver ordering.

2. **Audio transceiver direction**: Uses `sendrecv` (not `recvonly`) even when no audio track exists. This ensures `ontrack` fires on the remote side, enabling audio level detection and mute state indicators.

3. **Answerer path uses `addTrack`**: Only `addTrack`-created transceivers are eligible for m-line matching during `setRemoteDescription`. The answerer pre-attaches audio with `addTrack` and lets `setRemoteDescription` create the two video transceivers, then explicitly sets their directions to `sendrecv` BEFORE `createAnswer()`.

4. **Glare handling (perfect negotiation)**: Lexicographic ID comparison (`myId < peerId` = polite). Polite peer yields on collision by closing and recreating the connection as answerer (@roamhq/wrtc doesn't support rollback). Impolite peer ignores incoming offer.

5. **SDP modification**: our own description gets `stereo=1;sprop-stereo=1` and `maxaveragebitrate` from `audioReceiveKbps` (default 128 kbps — per RFC 7587 that field constrains the *remote* encoder), plus RED promoted ahead of bare Opus. The peer's description is rewritten with `audioSendKbps` before `setRemoteDescription`, which is the only lever on our own encoder.

6. **Connection retry**: Failed connections retry with exponential backoff (1s, 2s, 4s, max 3 attempts). Only the impolite peer (larger ID) retries to avoid simultaneous retry storms.

7. **Screen share via transceiver 2**: `setScreenTrack()` targets the 3rd transceiver (index 2), using `replaceTrack()` + direction toggle (`recvonly` ↔ `sendrecv`) and renegotiates. The renegotiation offer SDP is munged to force `a=sendrecv` on the screen m-line because @roamhq/wrtc may not reflect direction changes.

8. **Webcam via transceiver 1**: `setVideoTrack()` explicitly targets transceiver index 1 to avoid accidentally touching the screen transceiver. Camera mute sends black frames instead of renegotiating.

9. **Screen share state signaling**: `screen-share-state` WebSocket message broadcasts who is screen sharing. Re-broadcasts on `participants.length` change so newcomers learn current state. Remote screen windows close when `isScreenSharing: false` arrives.

### Per-peer latency estimation

The terminal client estimates one-way audio latency per peer using WebRTC stats already collected in the stats polling loop:

`estimated_latency ≈ RTT/2 + max(jitter × 2, 20ms) + 20ms`

- **RTT/2**: network one-way delay from `candidate-pair.currentRoundTripTime` and `remote-inbound-rtp.roundTripTime`
- **jitter × 2**: jitter buffer estimate (floor 20ms) from `inbound-rtp.jitter`
- **20ms**: fixed processing overhead (capture frame + encode/decode + playback FIFO)

Displayed in participant list as `~Xms` with color coding: dim (≤80ms), yellow (81–150ms), red (>150ms).

### Remote mute detection

- **WebSocket `mute-state` messages**: Explicit mute state broadcast on join, toggle, and participant change
- **Stream track check**: Fallback when no mute state has been received yet

## Package: terminal (`packages/terminal`)

Terminal UI (TUI) client for OpenMeet — join rooms, audio chat, video, screen share and text messaging from the terminal. Published to npm as `openmeet-terminal`.

### Two processes: TUI and engine

```
openmeet (TUI process)                     openmeet --engine (child process)
  Ink rendering, keyboard, settings          WebSocket signaling
  hooks/use-room.ts (thin IPC client)        PeerConnectionManager (wrtc)
        │ commands (join, mute, chat, …)     AudioManager (audify capture → mix → playback)
        ├──────────────────────────────▶     VideoManager (ffmpeg / ffplay)
        ◀──────────────────────────────┤     stats poll, debug log file
        state snapshots (on change), chat,
        room events, mic-test levels
```

The engine is the same bundle forked with `--engine` (`engine/client.ts`), started at app launch and killed when Ink unmounts. Everything with a 10 ms deadline lives in the engine; the TUI can stall for a full second and audio is unaffected. `hooks/use-room.ts` keeps the exact return shape the components always had, so UI code never touches wrtc, audify or ffmpeg. Device listing and the picker's mic test / test tone also go through the engine, so the native audio module is never loaded in the TUI process.

### Key files

| File | Purpose |
|------|---------|
| `src/index.tsx` | CLI entry point: arg parsing, audio backend selection, sox/mic checks (sox backend only), Ink `render()` with `alternateScreen` |
| `src/app.tsx` | App shell: `FullScreen` (the terminal-sized frame; `closeBottom={false}` hands the last row to a screen that draws its own bottom edge, which only the room does) and screen routing (profile on first start → home → device picker → room). `ProfileSetup` runs once, when settings hold no name or colour, and before a `--room` direct join too. Joining is the only way in — the server's `join-room` calls `ensureRoom`, so typing a room nobody is in creates it. There is no separate create step |
| `src/version.ts` | App version: build-time `__APP_VERSION__` via esbuild `define`, runtime fallback reads `package.json` |
| `build.mjs` | esbuild bundler: ESM, Node 22, bundles source + shared, externals for deps, injects `__APP_VERSION__` |
| `install.sh` | Curl-pipe installer for macOS/Linux (checks Node 22, warns about sox, then `npm install -g`) |
| `install.ps1` | PowerShell installer for Windows (installs Node LTS via winget if missing, `npm install -g`, ffmpeg via winget `Gyan.FFmpeg` if missing (screen sharing), then registers the Windows Terminal profile and desktop shortcut) |
| `windows/wt-profile.cjs`, `windows/create-shortcut.ps1`, `windows/openmeet.ico`/`.png` | "Own window" on Windows: an OpenMeet profile + theme in Windows Terminal (tab row and padding painted in `theme.bg`, the app's black, as the title bar; app icon; `closeOnExit: always`) and a desktop `.lnk` running `wt -w new --size 110,34 -p OpenMeet`. Shipped in the npm package (`files`). Limits: WT cannot hide the new-tab/dropdown buttons nor change the taskbar icon; the theme is global to the user's terminal |
| `src/engine/protocol.ts` | IPC contract: `EngineCommand` (TUI → engine), `EngineEvent` (engine → TUI), `RoomState` snapshot, `InputOptions` (channel policy + gain, sent with `join` and `mic-test-start` so the picker's meter shows what a call would send) |
| `src/engine/room-engine.ts` | `RoomEngine`: the room session without React — signaling, WebRTC, audio, video, stats, debug log. Snapshots coalesced to one per 100 ms |
| `src/engine/main.ts` | Engine process entry: IPC dispatch, mic test / test tone, crash → `fatal` event, exits on IPC disconnect |
| `src/engine/client.ts` | TUI-side `EngineClient`: forks the bundle with `--engine`, `send()`, `subscribe()`, `listDevices()`, `dispose()` |
| `src/lib/window-state.ts` | Render-pause policy (`never`/`minimized`/`unfocused`, setting `pauseRendering`, flag `--pause-rendering`). `minimized`: a long-lived helper (PowerShell + Win32 `IsIconic` on the hosting Windows Terminal/console window; `osascript` reading `miniaturized` from Terminal.app/iTerm2) prints state changes. `unfocused`: terminal focus reporting (`ESC[?1004h`), with stdin proxied through a filter because Ink 7 would read `ESC[I`/`ESC[O` as Escape. Emits through the `visibility` emitter; `use-room` holds snapshots/chat/events while hidden and the engine pauses level/stats polling (`set-visible`) |
| `src/lib/platform.ts` | `getPlatformSupport()`: per-OS policy (name, features label, `video` = ffmpeg pipeline available, `webcam` = camera capture implemented, default audio backend). One package version for all platforms; this is how the UI and the CLI express what that version enables on the current OS |
| `src/lib/diagnostics.ts` | Shared diagnostics: `diagnosticsEnabled()` (`--debug` or `OPENMEET_LOG=1`), `startLoopDelayMonitor()`, Ink render timings from the `onRender` hook |
| `src/lib/audio/tone.ts` | `ToneGenerator`: test tone frames for the device picker and the smoke script |
| `scripts/audio-matrix.ts` | Opens every input and output device the backend sees, reports native rate/channels, frames per second, per-channel RMS and imbalance, and driver errors. Run on any new machine: `pnpm exec tsx scripts/audio-matrix.ts [rtaudio\|sox] [seconds]` |
| `scripts/resampler-test.ts`, `scripts/channels-test.ts`, `scripts/voice-gate-test.ts`, `scripts/sdp-test.ts`, `scripts/nvidia-broadcast-test.ts`, `scripts/share-probe.ts`, `scripts/theme-test.ts`, `scripts/chat-test.ts`, `scripts/room-frame-test.tsx`, `scripts/children-test.ts`, `scripts/update-test.ts` | Pure, device-free checks: the resampler (SNR per ratio), the channel policy, the voice gate (silence and steady noise never open it, a voice always does, a quiet voice in a quiet room does too, the attack survives, a 200 ms pause does not cut the sentence), the Opus negotiation helpers against real wrtc SDP, the NVIDIA Broadcast name matching, the palette (4.5:1 on the background, and every meaningful pair still distinct after chalk's 256-colour rounding), the chat panel (renders the real `ChatLog` through Ink into a fake 60x8 terminal and drives it with key bytes: tail-following, wrapping, ↑/Page Up with the hidden count, a new entry not moving a scrolled view, ↓/Page Down resuming, arrows ignored when the input is unfocused, and `mergeChat`'s ordering), the room's frame (the last row before the border is content, not a rule stacked on it, and a `footer` brings the rule back), the child-process gate (a runaway loop is cut off, the cooldown is per kind, a deliberate restart is forgiven, nothing is left running), the update logic (version ordering, where 0.5.10 beats 0.5.9, and what counts as a copy we may replace), and the screen sender — two real `PeerConnectionManager`s in one process, asserting that a share started mid-call and one present at join both encode (gotcha 33). All run in CI on the three OSes |
| `scripts/audio-cost.ts` | Where an audio-only call's CPU goes, with no devices and no video: the 10 ms frame pipeline alone, plus `RTCAudioSource`, plus a connected PeerConnection carrying nothing, plus one sending a track, plus its decode, then three peers — and the same with mono/32k/DTX. It is what established that the codec is under 1% of a core and a PeerConnection is seven (docs/performance.md). `pnpm exec tsx scripts/audio-cost.ts [seconds-per-phase]` |
| `scripts/tui-cost.tsx` | What one frame of the room costs: the real room tree rendered into a fake 120x34 terminal, driven first the way a VU meter drove it (10 Hz) and then the way the speaking dot does. Reports renders/s, CPU, ms per render and bytes per frame. `pnpm exec tsx scripts/tui-cost.tsx [seconds-per-phase]` |
| `scripts/av-bench.ts` | A/V pipeline benchmark: drives the real `VideoManager` on both sides of a loopback PeerConnection while the real audio frame pipeline runs at 10 ms, and reports how often the audio gap exceeds 30 ms. `pnpm exec tsx scripts/av-bench.ts [seconds] [screen]`, `OPENMEET_BENCH_NOISE=1` to include RNNoise. Baselines and results in `docs/performance.md` |
| `scripts/pose.tsx` | The room posed for a photograph — `chat-demo`'s sibling, holding still: a call already under way, fixed meter levels, a clock that does not tick, and every state worth showing at once (a screen share you are watching, a camera on, someone muted, a peer at 70%, latencies across all three colours). `tab` lights the keycaps or gives you the composer with its cursor; `q` quits. `pnpm --filter openmeet-terminal exec tsx scripts/pose.tsx`, best at about 120x30. For the README and the npm page |
| `scripts/home-preview.tsx` | The home screen on its own, live: the real `HomeScreen` inside the real `FullScreen`, with no engine, no devices and no network. Run it under `tsx watch` (`pnpm --filter openmeet-terminal exec tsx watch scripts/home-preview.tsx`) and the process restarts on every save — the closest thing a terminal app has to hot reload, and it beats launching the app because there is no engine to fork and no state to walk back to. `u` cycles what the updater has to say (nothing, downloaded, not ours to install, just updated — remounted each time so the `✓` tick plays again), `q` quits |
| `scripts/chat-demo.tsx` | The chat, live, with no server: the room's real layout in your terminal, four people with names of different lengths and colours, and a scripted conversation arriving one line at a time (short, wrapping, a word longer than the column, joins, mutes, a screen share, a notice, a leave). ↑↓ / Page Up/Down scroll through the room's own code path while lines keep arriving, space pauses, r restarts, q quits. `pnpm --filter openmeet-terminal exec tsx scripts/chat-demo.tsx [ms between lines]`. For looking; `chat-test.ts` is the assertion |
| `scripts/audio-smoke.ts` | Audio backend smoke test: lists devices, captures for N seconds, plays a tone. `pnpm exec tsx scripts/audio-smoke.ts [rtaudio\|sox] [seconds] [dump.s16le] [gain]`; `OPENMEET_SMOKE_INPUT/OUTPUT` pick devices by name, `OPENMEET_SMOKE_TIMING=1` prints callback-interval histograms |

### Components

| Component | Purpose |
|-----------|---------|
| `home-screen.tsx` | Join a room; shows who you are as `[name]` in your colour, and is where an update speaks: `→ v0.6.0` beside the version, `Update downloaded. Restart to install.` with `r` under it, and a green `✓ updated` for four seconds on the first run after one installed itself (see `lib/update.ts`) |
| `profile-setup.tsx` | The first start, once, and the settings' Profile row: a name (typed, clamped live to `NAME_MAX_CELLS`), then a colour picked on a preview of that name. `initial` pre-fills, `onCancel` makes Escape leave |
| `name.tsx` | `Name`: a participant's name the one way it is ever shown — `[name]`, brackets included, in their colour (hashed from the name when a peer sent none) |
| `color-picker.tsx` | `ColorPicker`: the `NAME_PALETTE` rows, each previewing the name in that colour; ↑↓ and Enter, the caller owns Escape |
| `device-picker.tsx` | Audio input/output device selection (remembers preferences) |
| `room-view.tsx` | Main room. Header (version — with `→ v0.6.0` after it when an update is waiting, and nothing else about it: what to do belongs on the home screen — room, count, `Elapsed`, link stats), then `SplitPanes`: the chat on the left — the conversation and the composer, which brings its own rule — and the participants on the right, with the debug panel under them while debug mode is on. The screen and camera pickers are `Modal`s over it; the audio device change is a `Screen`. `q` twice leaves, drawn in the header — Escape is for cancelling a modal and clearing a draft, never for ending a call |
| `split-panes.tsx` | `SplitPanes`: the room's body, chat pane flexible on the left, people pane at `PARTICIPANTS_WIDTH` on the right, with the `┬` rule above — and a `┴` one below only when a `footer` is passed — built from the same two sizes so the junctions land on the divider at any width. Without a footer the panes close the frame themselves (`BottomEdge`: a `Rule` carrying the frame's own `╰`/`╯` corners and the divider's `┴`), because a child cannot bleed *downwards* onto the frame's border the way `Rule` bleeds sideways onto it — `FullScreen`'s `closeBottom` leaves that row to the room. The rule that used to sit above the border separated the panes from the bottom bar, and once the bar was retired it enclosed nothing, which reads as an empty strip; `scripts/room-frame-test.tsx` pins that it is gone and that the `┬` and `┴` share the divider's column. The demo passes its keys as a footer and keeps an ordinary rule. The divider is the chat pane's own right border, so a `Rule` inside either pane draws its junction on it (a child may draw over its parent's border; Ink draws the border first) — but only from a box with no horizontal padding, since the rule's one-column bleed and a padding of one cancel out and strand the junctions a cell inside. Shared by the room and `scripts/chat-demo.tsx`, so the demo shows the real layout |
| `camera-picker.tsx` | `CameraPicker`: which camera to share, over the room, with `t` to look through one first — a list of names says little about which lens is which. The room releases the capture before opening it (`set-video-device: null`) because on macOS a camera opens once, so a preview and a capture cannot both hold it, and hands it back on cancel. `cameraBusy` (from `RoomState.webcamCapturing`) holds `t` until the grabber has actually exited — asking it to stop is not the same as it having stopped. A preview that shows nothing reports what ffmpeg or ffplay said, and translates the usual `Input/output error` into what it means: another app has the camera |
| `modal.tsx` | `Modal`: a panel over the room rather than a screen instead of it, so the conversation and the participants stay visible behind the choice. Ink has no z-order, but it draws the tree in order and Yoga positions absolutely, so an absolutely positioned overlay placed last covers what came before; the panel paints its own background. The room renders it alongside its own tree and everything under it stands down (`active` on `ChatLog` and `ChatInput`, and the key handler returns early); `onKey` is where the panel answers its own keys |
| `screen.tsx` | `Screen`: every full-height screen but the room — title on the first row, `Rule` from border to border, padded content, key hints on the last row. Owns the padding, so `Rule` needs no notion of it; title, rule and hints never shrink, the content clips |
| `elapsed.tsx` | `Elapsed`: time in the room as a leaf, so its tick re-renders one Text; coarser as it grows (`formatElapsed`) so it stops costing a frame a second after the first minute |
| `chat-input.tsx` | The chat's composer: its own `Rule`, then the prompt, the draft and `tab` on the right. The prompt's box never shrinks (squeezed, it loses the space after `>` and the message starts against it) and the draft's box grows and shrinks, so a long message wraps in its own column instead of running under the chip. `esc esc` throws the draft away rather than holding backspace down a long message: two presses, armed for 2 s, disarmed by any other key, and the offer is a line above the rule that only appears once you have pressed Escape — a chip in the row would spend a fifth of its width advertising a rare key. The draft stays visible while unfocused |
| `chat-log.tsx` | The conversation: messages and room events (joins, shares, mutes, errors) folded into one stream by time — `mergeChat` merges the two already-ordered lists, `ChatEntry` is a flat line (`kind`, `who`, `color`, `text`); debug lines are excluded. Every line has the same shape, `[time] icon who what`: the icon names the kind in its colour (`›` message, `+` here, `-` gone, `▣` screen, `♪` mute, `·` notice), `who` is `[name]` in that person's colour followed by one space (a room-wide notice has none); a message's colour comes from the server, an event's from the engine, which has the participant in hand when it writes the event (`RoomEvent.who`/`color`) — including someone leaving, and event text is muted so the conversation stands out; the engine sends `who` as its own field on `RoomEvent`. Scrolls by entry, anchored on the last visible one, so a message arriving while you read older ones does not move the view; a muted line counts what is below. ↑/↓ scroll while the input has the focus, Page Up/Down always. Layout is Ink's: the entries up to the anchor go into a `flex-end` box that clips vertically, one `Text` per entry, so a long entry shows its tail (gotcha 34) |
| `participant-list.tsx` | The participants column, one line per person: speaking dot, selection marker, `[name]` and state tags on the left, numbers on the right. **The dot is the whole of the audio display, and it is the voice gate** — filled while that person is reaching the room, hollow while they are not. A 10-cell VU meter used to sit at the end of each row, redrawn ten times a second; in a terminal that means rebuilding and reserialising the whole frame ten times a second, measured at 31 ms of CPU per frame and the single largest thing the TUI did (docs/performance.md, Sept 2026). A level meter earns that while you are choosing a microphone, which is where it still lives (`MicBar` on the device picker). Tags are single letters run together with no background — `mcs`, mute orange, cam purple, screen blue — so brackets mean a name and nothing else; the case says whether you have that peer's window open (lower while they send, upper while you watch), and the volume shows only when it is not 100%. You come first with no `(you)`, with your own keys under you: a `Divider` inside the padding groups your row with them, and a `Rule` below, outside it, is the section break between you and everyone else. `PARTICIPANTS_WIDTH` is measured with `string-width` from the widest line it can hold — a name at `NAME_MAX_CELLS`, every tag, three-digit numbers, a volume other than 100% — so nothing wraps and the chat, the flexible side, never reflows when a tag appears; it follows that rule rather than a number, which is why dropping the meter handed the chat ten columns on its own. A peer's name is clamped to the same limit on display, whatever their client sent |
| `status-bar.tsx` | The room's keys, drawn where the thing they act on is, all from `key-hints.tsx`: `MyActions` (`m`/`d`/`s`/`v`) under your own row in the participants column and `PeerActions` (`↑↓`/`-/+`/`w`/`e`) pinned under the list — the chat's `tab` chip lives in `chat-input.tsx`, and leaving (`q`, twice) in the header. There is no bottom bar: `g` (debug panel) still works and is deliberately undrawn, and the overlay is a settings row rather than a room key. Every row greys out whole while the chat input has the focus, and `PeerActions` keeps its keys and labels whatever the selected peer is doing, so the list never jumps; whether you already have their window open is in their tags, not in the label |
| `level-bar.tsx` | `levelColor` (the meter's one threshold ladder) and `MicBar` (the mic-test meter, 30 cells) — the one meter left in the app, on the device picker, because choosing a microphone is the one moment a continuous level is worth a redraw. It draws a `Bar` in eighth-blocks (`▏▎▍▌▋▊▉█`) in the level colour over a `theme.surface` track, so a partial cell is the same two colours as its neighbours. In the room the audio display is the speaking dot (`participant-list.tsx`) |
| `select.tsx` | `Select`: `ink-select-input` with our own indicator (`▸` in the accent, like the settings menu) and item (bold when highlighted, theme foreground otherwise). The stock ones draw `color="blue"` and no foreground at all. The only file allowed to import the package (Biome `noRestrictedImports`) |
| `text-input.tsx` | `TextInput`: our own single-line input (arrows, backspace, enter, placeholder in the muted grey) with a **blinking cursor** — an inverse-video cell toggled every 530 ms in React while focused, restarted on each keystroke, stopped while the window is hidden. It replaced `ink-text-input`, whose text had no foreground, whose placeholder was `chalk.grey`, and whose only cursor switch also disabled arrow editing; Biome refuses the package |
| `text.tsx` | `Divider`: a separator within a panel, as wide as its container and no wider, so it stays clear of the frame and of any divider beside it (the participants column separates you from everyone else with one). `Line`: one run of `─` with an optional junction glyph at either end, built as a Box with a custom top border so Yoga sizes it (never a `repeat`-ed string, gotcha 30d). `Rule`: a section rule from frame edge to frame edge, its junctions (`├` and `┤` by default, the frame's `╰`/`╯` when the rule *is* the frame's bottom edge) drawn over the frame's border by bleeding one column past its container — so it lives directly in an unpadded column (`Screen`, `SplitPanes`); pass `Line` children that mirror a split's sizes and the `┬`/`┴` land on the divider's column at any width. Relies on `FullScreen` clipping vertically only (`overflowY`), because Ink clips at the border. `POINTER` (`▸`) and `Pointer`: the one selection marker, as the row's marker cell |
| `text.tsx` | `Text` with the theme applied. Ink inherits a background down the tree but not a foreground, so a bare `<Text>` would render in the terminal's default colour — black, on a light theme, over our dark background. Defaulting it here is what keeps that from being a bug in every new line of UI. `dimColor` is accepted but remapped to an explicit grey |
| `key-hints.tsx` | `KeyHints` (a row of footer buttons) and `KeyChip` (one key inline in a sentence). Two-tone buttons: the key bold on a gold keycap, what it does on a grey `theme.surface` pill right after it, one space between buttons: ` m `` mute `, ` s `` share screen `. Chosen over a single gold block in Sept 2026 so the bar stops competing with the frame. The chip sets its own foreground and background so it reads on any terminal theme; the palette is fixed because Ink cannot detect the terminal background. A hint with `disabled` has both halves on `theme.surface` in the muted grey rather than being dropped, so the bar never reflows; the room's bar disables everything but `esc` and `tab` while the chat input has the focus (`inputFocused`, the same flag the key handler gates on). Every bottom-bar hint row uses it, so they stay identical |
| `debug-log.tsx` | The `[DBG]` lines only, under the participants while debug mode is on (`g`). Every other room event goes into the chat |
| `settings-view.tsx` | Settings screen: Profile first (the same two `ProfileSetup` steps as the first start, applied on next join), then the device pickers (one `Screen`, a table of three), overlay, channels, noise suppression, bitrates, updates, render pause |

### Hooks

| Hook | Purpose |
|------|---------|
| `use-room.ts` | Thin binding over the engine process: sends commands, mirrors `RoomState` snapshots, chat and room events into React state. Logic lives in `engine/room-engine.ts` |

### Lib modules

| Module | Purpose |
|--------|---------|
| `websocket.ts` | WebSocket client for signaling (auto-reconnect with backoff, pub/sub) |
| `webrtc.ts` | `PeerConnectionManager` with `@roamhq/wrtc` |
| `audio/backend.ts` | `AudioBackend` interface, `AudioDevice`/`AudioDeviceSelection`, backend selection (platform default from `lib/platform.ts`, `--audio-backend` overrides via `parseBackendFlag`) |
| `audio/rtaudio-backend.ts` | Native backend via audify/RtAudio (default on macOS and Windows): duplex stream when the device runs at 48 kHz, otherwise split streams with capture at the device's native rate (libwebrtc resamples); re-chunks driver periods to 10 ms; playback queue kept at ~20 ms |
| `audio/sox-backend.ts` | Fallback backend (Linux default): `rec` pipe for capture, one `play` process on a FIFO for the mixed output paced by wall-clock; async device listing via `system_profiler`/`pactl` |
| `audio/manager.ts` | `AudioManager`: backend-agnostic pipeline — capture → mute → `CaptureProcessorChain` → **`VoiceGate`** → `RTCAudioSource`; `RTCAudioSink` per peer → `PeerPlayoutBuffer` → `FrameMixer` → backend. Auto-restarts the backend on device errors (3 attempts, then defaults). Muting holds the gate shut without feeding it, so a muted participant costs every peer nothing at all, and `ready` resolves on the first *captured* frame rather than the first transmitted one — with the gate shut a silent room would never report itself started |
| `audio/voice-gate.ts` | `VoiceGate`: whether we are on the air. A call is mostly silence, and in a mesh silence costs what speech costs — our encoder runs, a packet goes to every peer, every peer decodes it back into nothing. While the gate is shut nothing is pushed to WebRTC, so nothing is encoded, sent or decoded anywhere in the room. Two things make it safe in front of a microphone: it measures the room's noise floor (only while shut — measuring during speech would let it raise its own bar until it never opened) and opens ~10 dB above it, never above `SPEAKING_RMS_THRESHOLD` so it is never stricter than the dot's old threshold and never below `MIN_OPEN_RMS` so digital silence cannot open it; and it keeps a 40 ms ring of frames behind it that go out ahead of the first loud one, so the consonant that started the word is not clipped. Closing has hysteresis and a 300 ms hold. Off with `--no-voice-gate` or the Settings row; `scripts/voice-gate-test.ts` pins the behaviour |
| `audio/noise-suppression.ts` | RNNoise as a `CaptureProcessor`, opt-in via `noiseSuppression`. 0.22 ms/frame when both channels match (any mono mic), 0.45 ms when genuinely stereo — each channel gets its own denoiser so a stereo source keeps its image |
| `audio/nvidia-broadcast.ts` | NVIDIA Broadcast detection (Windows + RTX). Its virtual mic is an ordinary WASAPI endpoint, so nothing is wired for it to *work*; this names it. `isBroadcastDevice`/`findBroadcastInput` match the driver name (locale-proof — Windows translates only the role prefix), `preferBroadcastFirst`+`inputDeviceLabel` shape the pickers, `resolveBroadcastDefault` names it when it is the *system default* (a selection of `undefined` carries no name, so nothing downstream could otherwise tell), and `hasRtxGpu()` is a cached async PowerShell probe used **only** to suggest Broadcast to someone who has an RTX and hasn't installed it. Runs in the TUI process, never the engine |
| `audio/processors.ts` | `CaptureProcessor` interface + chain. **This is the hook for noise suppression** (RNNoise via `@shiguredo/rnnoise-wasm`): a processor gets each 10 ms stereo frame before WebRTC. Add it with `AudioManager.addCaptureProcessor()`; add a settings flag only when the processor lands |
| `audio/mixer.ts` | Per-peer ring buffer (12 frames, 2-frame prefill, drops oldest on overflow) and int32 mixing with per-peer gain |
| `audio/resampler.ts` | Streaming polyphase (Kaiser-windowed sinc) rational resampler, Int16 interleaved, any chunking. ≈90 dB SNR, ~0.5% CPU. Used by the RtAudio backend both ways so streams open at each device's native rate |
| `audio/channels.ts` | `InputConditioner`: channel policy (`auto` detects a mono mic in a stereo pair after 1 s of signal with ≥10 dB imbalance; `stereo`/`mono`/`left`/`right` forced) and input gain in dB with clamping. Also `INPUT_CHANNEL_POLICIES`, `parseChannelsFlag`/`parseGainFlag` (CLI validation) and `imbalanceDb`. Reset on every backend (re)open |
| `audio/pcm-dump.ts` | Diagnostic recorder: `OPENMEET_DUMP_DIR=<dir>` writes raw s16le stereo of the capture (as pushed to WebRTC), each peer's decoded audio (`sink-<id>`) and the mixed output; `OPENMEET_TEST_TONE=1` replaces the mic with a 440 Hz tone |
| `audio-test.ts` | Mic level meter and test tone for the device picker, built on the active backend |
| `video.ts` | VideoManager: ffmpeg webcam + screen capture (send), ffplay display (receive), overlay. `FrameAssembler` fills one preallocated frame from the pipe instead of growing a Buffer (5.22 → 0.03 ms/frame). Receive is 1:1: ffplay is spawned at the source resolution and respawned if it changes, so nothing is rescaled in JS |
| `children.ts` | The one gate every long-lived child goes through (`spawnChild`), and the one way they are killed (`killChild`: SIGTERM, SIGKILL behind it, resolving when the process is really gone). It exists because a bounded retry counter was not enough: a camera that would not open was reopened several times a second, each turn spawning ffmpeg, until the machine ran out of memory and had to be power-cycled. `spawnChild` returns `null` — which every caller already handles, since a process can always fail to start — when 12 children are alive or one kind has burnt 6 spawns in 10 s; either way that kind then waits 30 s, and the refusal is logged **once** per cooldown, because the logger is an `appendFileSync` on the engine's audio loop and a refused loop would otherwise write at loop rate (the original symptom in a worse place). `forgiveChildKind` clears the budget when a *person* asks for something, never a retry path. Out of scope by design, and stated here because the invariant should be checkable: the sox backend's `rec`/`play` and the minimized-window watcher spawn outside the gate. `killAllChildren` runs on engine exit and on `process.exit`, so nothing of ours outlives us holding a camera. `scripts/children-test.ts` drives it the way the runaway loop did |
| `preview.ts` | `startPreview(capture, player)`: an ffmpeg capture piped into an ffplay window, returning the function that closes it. The caller owns the lifetime because the preview holds the device — `--test-camera`/`--test-screen` and the camera picker all go through it |
| `tool-path.ts` | `findTool(name)` / `ffmpegBin()` / `ffplayBin()`: where a tool is, resolved once per process — the PATH first (`where`/`which`), then on Windows `%LOCALAPPDATA%\Microsoft\WinGet\Links` and `Program Files\ffmpeg\bin`. Every spawn site uses these, never a bare name (gotcha 31); the engine calls `warmTools()` at join so its cache fills without a synchronous spawn next to live audio |
| `powershell.ts` | `runPowerShell(script)` / `powershellArgs(script)`: the one preamble and exec policy for PowerShell probes (screen enumeration, the RTX check) |
| `capture-args.ts` | `webcamCapturePlan(device)` in `devices.ts` is what callers use — the arguments and the frame size they will produce, decided together because the raw pipe has no header and the reader slices by length. Under it: `webcamCaptureArgs(device, size)` (avfoundation / v4l2, `null` where webcam is unsupported; the size is a scale+pad filter, never `-video_size` — gotcha 12), `pickWebcamMode(modes)` (the smallest mode covering 720, at the best rate up to 30 — *asking* matters: left alone, avfoundation hands over the camera's default, 3840x2160 on an Insta360 Link, so the USB carries 4K and every frame is resized; measured 2.65 s of CPU per 5 s against 0.98 s when 720p is asked for) and `webcamOutputSize(mode)` (that mode's aspect ratio, height ≤ 720 and width ≤ 1280, never enlarged — a fixed 4:3 frame letterboxed every 16:9 camera). Both it and `screenOutputSize` fit through one `fitUnderCaps`, so the never-enlarge/even-sides rule lives once, `screenOutputSize(device)` (the share's shape: the screen's aspect, short side ≤ 1080, long side ≤ 3840 — gotcha 17), `screenCaptureCandidates(device)` — ffmpeg argument sets best first (avfoundation / **ddagrab** then gdigrab / x11grab, I420 output at exactly that size, 30 fps); the caller walks the list when one yields no frames, which is how an RDP session falls back off DDA and `rawVideoPlayerArgs()` for ffplay — which caps the *window* at 1280x720 with `-x`/`-y` (`playerWindowSize`, downscale only) so a 1080p share does not open edge to edge on a 1080p monitor, while the stream keeps its own resolution. Plus the size constants. wrtc-free so the CLI's `--test-camera` / `--test-screen` run exactly what a call runs |
| `overlay.ts` | 8×8 bitmap font overlay renderer burned into the I420 frames of the video windows you open — the peer's name and stream type, not a preview of your own camera. Toggled in settings (`videoOverlay`) |
| `devices.ts` | `probeCameraModes(device)`: the resolutions and rates a camera declares, got by demanding a size no camera has (`1x1`) so avfoundation refuses and prints its list — the only way, since `-list_devices` gives names only, and the mode must be chosen before the capture starts because the pipe carries raw frames with no header. ~0.35 s, macOS only (the reply is avfoundation's), and it never opens a stream so it does not hold the camera. Cached per device *including* the empty answer — an unopenable camera is exactly what would otherwise re-probe, and wait out the timeout, on every attempt; `forgetCameraModes` drops the cache when a camera is reconfigured under us. Plus video/screen device enumeration (macOS avfoundation, Linux xrandr, Windows PowerShell `Screen.AllScreens` with DPI awareness). A screen carries the monitor's own name where the OS knows it — on macOS `system_profiler` supplies it ("Odyssey G85SB"), since ffmpeg only ever says "Capture screen 0" — plus its size and whether it is the main display, which is what the picker shows. Screens are cached 60 s and `prefetchScreenDevices()` warms the cache when a room opens, because the PowerShell call costs ~1 s and runs synchronously in the TUI on the `s` key |
| `theme.ts` | The palette, in 24-bit hex. Named ANSI colours are palette *indices*, so `color="yellow"` is a different hue in every terminal theme; `dimColor` (SGR 2) is reduced opacity, a substituted palette entry, or nothing, depending on the emulator. Hex renders as `38;2;R;G;B` on truecolor and quantises to indices ≥ 16 on 256-colour terminals, and those are fixed by the xterm spec rather than the theme — so Ghostty, iTerm2 and Windows Terminal show the hex and macOS Terminal (256-colour only) the nearest cube entry — chalk's nearest, which can sit one step off in hue; `scripts/theme-test.ts` checks that no meaningful pair merges there. Only a 16-colour terminal falls back to the theme. `users` (chat names) share the state colours' lightness but none of their hues |
| `settings.ts` | Persistent settings (`~/.config/openmeet/settings.json`) |
| `update.ts` | Staying up to date. The registry (`registry.npmjs.org/openmeet-terminal/latest`, 3 s timeout, answer cached a day in settings) is asked in the background at launch; if there is a newer version and this copy is ours to replace, the tarball goes into npm's cache (`npm cache add`, which touches nothing that is loaded) and only *then* does the home screen say so — the notice never promises an install it has not already downloaded. The install itself can only happen once the app is gone, because the engine holds `audify.node` and `wrtc.node` from launch to exit and Windows will not replace a directory in use: `r` installs in the foreground and starts the new bundle with the same arguments, quitting hands it to a detached `--apply-update` process writing to `update.log`. Both verify (the package on disk really changed version, then `require('audify')`/`require('@roamhq/wrtc')` load), retry once with `--allow-scripts=audify` — the repair `install.ps1` already does — and put the previous version back if it still fails. `autoUpdate` in settings and `--no-auto-update` turn it down to a notice or off |
| `sdp.ts` | Opus negotiation: `boostOpusQuality` (our description — stereo, and the ceiling peers may spend sending to us), `capOutgoingAudioBitrate` (the peer's description — our own send ceiling), `preferAudioRed` (RED ahead of bare Opus). Default 128 kbps each way; `AUDIO_KBPS_STEPS` is what the settings screen cycles. Screen share: `setScreenBandwidth` (`b=AS` on the screen m-line — the one bitrate lever that is re-read on every renegotiation, gotcha 27b), `pixelFactor` (a wider share deserves proportionally more, ×1–2), `DEFAULT_SCREEN_KBPS` 2500, `SCREEN_KBPS_STEPS` |
| `identity.ts` | Who you are: `Identity` (name + colour), `NAME_MAX_CELLS` (8, in terminal cells — `string-width`, so CJK counts double), `NAME_PALETTE` (13 named colours: white, then round the hue wheel, all AA on the background), `clampName`/`finishName` (what typing and saving allow: no control characters or brackets, whitespace collapsed, cut at the cell limit), `colorForName` (the chosen colour, else hashed from the name), `bracketed`, `paletteEntry`. Settings-free, so the engine can import it: peer names are cut to the limit once, in `RoomEngine.cleanParticipant`, where they enter from the wire. `loadIdentity` is in `settings.ts` |
| `clock.ts` | `formatClock` (`HH:MM`, or with seconds) and `formatElapsed` (seconds, then minutes, then hours) — the two clocks every panel shares |

### Tech stack

- **Runtime**: Node.js >= 22
- **TUI framework**: Ink v5 (React for terminal)
- **WebRTC**: `@roamhq/wrtc` (native WebRTC bindings for Node.js)
- **Audio**: sox (`rec` for capture, `play` for playback)
- **Video**: ffmpeg (webcam + screen capture) + ffplay (remote video display)
- **Bundler**: esbuild (single-file ESM bundle with shebang)

### CLI usage

```bash
openmeet [options]
  --server <url>         WebSocket URL (default: wss://openmeet.mvega.pro/ws)
  --room <id>            Room ID to join directly
  --input-device <name>  Input device name (skip device picker)
  --output-device <name> Output device name (skip device picker)
  --no-video             Disable video (audio-only mode; webcam is macOS/Linux only)
  --audio-backend <name> rtaudio (native; default on Windows) or sox (default on macOS/Linux)
  --audio-send-kbps <n>  Opus ceiling for what we send (default 128, saved)
  --audio-receive-kbps <n>  Opus ceiling for what peers send us (default 128, saved)
  --screen-send-kbps <n>    Screen-share ceiling per peer at 1080p (default 2500, saved)
  --screen-receive-kbps <n> Screen-share ceiling we ask of each peer (default 2500, saved)
  --noise-suppression    RNNoise on the mic (--no-noise-suppression to turn off, saved)
  --no-voice-gate        Send continuously instead of only while speaking (saved)
  --video-device <id>    Video capture device (e.g., "0" for macOS avfoundation)
  --no-overlay           Disable video overlay
  --no-auto-update       Do not check for or install updates this run (setting: Updates)
  --test-camera          Test camera capture (opens ffplay preview, no room join)
  --test-screen          Test screen capture (lists screens, opens ffplay preview)
  --debug                Enable debug logging (also writes to ~/.config/openmeet/debug.log)
  -h, --help             Show help
```

### Publishing

Published to npm via GitHub Actions. See [CI/CD](#cicd) section.

## CI/CD

### Workflow: `ci.yml`

On every push to `main` and every PR: `pnpm install`, lint, build, `tsc --noEmit` on the terminal, and a smoke script that loads `@roamhq/wrtc` and `audify` on ubuntu, macOS and Windows runners (runners have no audio devices; enumeration must simply not throw).

### Workflow: `publish-terminal.yml`

Automates npm publishing of `openmeet-terminal` via GitHub Actions.

- **Trigger**: Push tags matching `terminal-v*` (e.g. `terminal-v0.1.0`)
- **Steps**: checkout → pnpm + Node 22 setup → install → build shared → build terminal → publish to npm → create GitHub Release
- **Auth**: none stored. npm **trusted publishing** — the job's OIDC token (`id-token: write`) is exchanged for publish rights against a trusted publisher configured on npmjs.com for this repository and this workflow's filename. Renaming the file or moving the repo breaks the publish until that is updated to match. The tarball is published `--provenance`, so npm shows which commit and which run built it
- **npm version**: the job installs `npm@latest` first. Trusted publishing needs npm ≥ 11.5.1 and `setup-node` ships whatever npm came with the Node release; `pnpm publish` rewrites the `workspace:` protocol and then delegates to the npm on PATH, so that is the one doing the exchange

### How to publish a new version

```bash
# 1. Bump version in packages/terminal/package.json
# 2. Commit and tag
git add packages/terminal/package.json
git commit -m "Release openmeet-terminal v0.2.0"
git tag terminal-v0.2.0
git push && git push --tags
# GitHub Actions builds, publishes to npm, creates a GitHub Release
```

## Code Style & Conventions

- **Linter/Formatter**: Biome v2.4+ (not ESLint). Config at root `biome.json`.
- **Formatting**: 2-space indent, single quotes, trailing commas, semicolons, 120 char line width
- **Iteration**: Use `for...of` loops, not `.forEach()` (Biome rule)
- **Imports**: Auto-organized by Biome. No `import type` enforcement (`useImportType: off`).
- **noExplicitAny**: Off. `noNonNullAssertion`: Off.
- **File exclusions**: `dist/` directories

## Scripts

| Command | Description |
|---------|-------------|
| `pnpm dev` | Start server (3001) + shared in watch mode |
| `pnpm --filter openmeet-terminal dev` | Run the terminal client against `ws://localhost:3001/ws` |
| `pnpm build` | Build shared → server → terminal (order matters) |
| `pnpm --filter openmeet-terminal exec tsx scripts/audio-smoke.ts rtaudio 3` | Validate a machine's audio stack without joining a room |
| `pnpm lint` | `biome check .` |
| `pnpm lint:fix` | `biome check --write .` |
| `pnpm format` | `biome format --write .` |

## Docker

- **Multi-stage build**: `node:22-alpine`, builds only `shared` + `server` (`pnpm install --filter "@openmeet/server..."` skips the terminal's native `@roamhq/wrtc` build)
- **pnpm prune**: Must use `CI=true pnpm prune --prod` (non-TTY environment)
- **Production**: Single container serves the REST API + WebSocket signaling on port 3001

## Known Gotchas

1. **pnpm prune in Docker**: Needs `CI=true` prefix for non-interactive environments.
2. **Audio `ontrack`**: Only fires if transceiver direction is `sendrecv` (not `recvonly`).
3. **Build order**: shared must build before server and terminal (workspace dependency).
4. **STUN servers**: Uses Google public STUN servers. TURN server needed for restrictive NATs.
5. **Transceiver ordering**: Both sides must end up with 3 transceivers in identical order (audio, webcam, screen) after SDP exchange. Do not reorder or skip.
6. **Screen share state re-broadcast**: Must re-broadcast on `participants.length` change so newcomers learn the current screen share state.
7. **Terminal npm publish**: no token. Publishing is npm trusted publishing over OIDC, so a release only happens from `publish-terminal.yml` in this repository — `npm publish` from a laptop is refused, which is the point. Tags must match `terminal-v*` to trigger it. Two traps this replaced: a stored `NPM_TOKEN` expires (npm's classic automation tokens were revoked in favour of granular ones capped at 90 days) and the failure reads as `404 Not Found - PUT …/openmeet-terminal`, which looks like the package does not exist rather than like an auth problem; and provenance is per version, so anything published by hand stays unsigned for ever.
8. **Terminal sox dependency**: only for `--audio-backend sox` (default on Linux; macOS and Windows default to RtAudio). The CLI checks for `rec`/`play` on startup only in that case.
9. **Terminal esbuild bundle**: `@openmeet/shared` is aliased and inlined, and everything else is bundled too **except** the three native modules and `ws` (`external` in `build.mjs`). That is a CPU decision, not a size one — see gotcha 36. The output is a single ESM file with a `#!/usr/bin/env node` shebang followed by a `createRequire` line, which is what lets the bundled CJS dependencies under Ink reach node builtins (esbuild's `__require` shim uses a real `require` when one is in scope and throws when it is not). Ink's `react-devtools-core` import is answered by an inert stub: it is loaded behind `DEV=true`, and being a static import inside a lazily-imported module esbuild hoists it to the top of the bundle, so without the stub the package would have to be installed for the app to start. `__APP_VERSION__` is injected via esbuild `define`; `src/version.ts` provides a runtime fallback for `tsx` dev mode.
10. **Terminal alt screen + Ink clearTerminal**: Ink writes `\x1b[2J\x1b[3J\x1b[H` when output fills the screen. The `\x1b[3J` (clear scrollback) leaks through the alternate screen buffer on macOS, wiping terminal history. `index.tsx` patches `process.stdout.write` to strip `\x1b[3J` (Ink now owns the alt screen via `alternateScreen: true`).
11. **Terminal video answerer SDP direction**: In the answerer path, `setRemoteDescription` creates transceivers defaulting to `recvonly`. Must explicitly set transceiver directions to `sendrecv` BEFORE `createAnswer()` (not via post-creation SDP munging). This applies to audio (index 0), webcam (index 1), and screen (index 2 when sharing).
12c. **Never pin a camera's mode with `-video_size`; scale cheaply instead.** ffmpeg's avfoundation input keeps the geometry it negotiated when the camera was opened. Anything that changes the device's format afterwards — the Insta360 Link Controller switching 720 to 1080, another app opening the camera larger — leaves it reading the new buffers with the old stride, and the picture comes out combed and doubled. It is not exclusivity and not a codec fault: macOS shares a camera happily, and ffmpeg says nothing on stderr when it happens. Reproduced 2026-09-09 by forcing 4K from a second capture while ours ran pinned at 720; unpinned, the same test stays clean. Measured cost of not pinning, from a 4K camera: 2.24 s of CPU per 5 s with the default scaler, **1.14 s with `-sws_flags fast_bilinear`** — the same as pinning cost (1.10 s), so the robustness is free. `probeCameraModes`/`pickWebcamMode` still run, but only for the *shape*: the aspect ratio to send. And a camera reconfigured from its own control app stops mid-stream, sometimes without its ffmpeg dying: `VideoManager` watches the frames (`WEBCAM_SILENCE_MS`, 5 s — a screen may be still, a camera never is) and reports through `onWebcamCaptureEnded`; the engine forgets the modes, reopens **once** (`MAX_CAMERA_RETRIES`, reset whenever the camera is started on purpose) and only while it is actually being shared. Both bounds were learnt the hard way: unbounded and ungated, a camera that would not open filled the room log with `camera stopped … reopening` several times a second. The camera is also no longer opened at join — it starts muted, and holding a camera nobody is sharing burns its light for the whole call and keeps it from other apps. `RoomEngine.setCamera(on, device?)` is the one place it opens and closes, serialized behind a promise: the picker used to send "use this camera" and "turn it on" as two commands, and the second could open the *previous* camera while the first was still stopping the old grabber. It is now one command, `share-camera`, and cancelling the picker does nothing because a camera that is off is not held.
12b. **A camera opens once on macOS, and the error for that is `Input/output error`.** OBS, Zoom or a browser tab holding a UVC camera (an Insta360 Link, say) makes every other opener fail that way — including our own capture and the picker's preview — and the message reads like a broken device. `camera-picker.tsx` translates it. Our own two holders are handled: `VideoManager.stopCapture()` resolves only when the grabber has *exited* (SIGTERM, SIGKILL 300 ms later), and the picker's `t` waits on `RoomState.webcamCapturing` rather than on a timer.
12. **Terminal video capture on macOS: ask for the rate, never the size.** avfoundation rejects `-framerate 15` despite listing it as supported — use 30. And `-video_size` is a *demand*, not a request: a camera without that exact mode fails to open at all, `Input/output error`, ffmpeg exiting before a frame. An Insta360 Link (1920x1440, 3840x2160, …) has no 640x480, so the forced size silently disabled it — the preview flashed and closed, and sharing it in a call would have sent nothing. `webcamCaptureArgs` now opens the camera at whatever it offers and scales+pads to `WEBCAM_WIDTH`x`WEBCAM_HEIGHT` in a filter, which is what the screen path already did. Dropping `-framerate` as well fails the same way, so the rate stays — but it is only a request, and when the camera's timestamps do not match it, ffmpeg duplicates frames to fill the gap: ~119,000 fps measured out of an Insta360 Link, enough to freeze the player and, in a call, to bury the engine's loop. So the filter chain starts with `fps=WEBCAM_FPS`, which paces the output whatever the camera does. The screen path must not have that (gotcha 28). Verified on this Mac: an Insta360 Link that failed to open now delivers frames; a Roland capture card with no signal fails either way (a device can open and send nothing — the picker says `sent no picture` rather than closing without a word). The two built-in cameras could not be measured from tooling, which blocks on the camera permission prompt.
13. **Terminal ffmpeg device listing**: `ffmpeg -list_devices` exits non-zero (because `-i ""` is invalid). Must use `spawnSync` (not `execSync`) to capture stderr without throwing.
14. **Terminal video windows manual**: Remote webcam (`w` key) and screen share (`e` key) windows are opened manually by the user on the selected peer. Screen windows auto-close when the peer stops sharing. `w` is gated on the peer having camera on (`remoteVideoMuteStates[peerId] === false`) or a window already being open; `e` on `remoteScreenShareStates[peerId]`. The status bar derives `peerCam`/`peerScreen` from exactly those conditions, so the `w` and `e` buttons appear only when they would do something and read `watch`/`close` for what the key would actually do — keep the two in step, a drawn button that does nothing is worse than no button. `s` follows `videoEnabled` for the same reason, and when video is off the engine writes *why* to the room log (`videoDisabledReason`, computed in `index.tsx`: `--no-video`, unsupported OS, or `ffmpeg and ffplay not found on PATH`) — the stderr warning is covered by the alternate screen the instant Ink starts, so this is the only place a user sees it. Also: the chat input has focus on entry, so hotkeys need `Tab` first; that is by design, not a dead key. Tracks are stored in refs and attached to VideoManager on demand.
15. **Terminal settings persistence**: Settings stored at `~/.config/openmeet/settings.json`. Includes `name` and `color` (null until the first-start profile screen sets them, which is how the app knows to show it), audio device IDs, video device ID, overlay toggle, `voiceGate`, the `devicesConfigured` flag, and the update fields (`autoUpdate`, `lastUpdateCheck`/`latestSeen` — the day-long cache of the registry answer — and `lastRunVersion`, which is the whole mechanism behind the `✓ updated` tick: the version that ran last not matching the one running now). Legacy flat files (`audio-input`/`audio-output`) auto-migrated on first read.
16. **Terminal screen capture macOS**: Uses ffmpeg avfoundation with `-capture_cursor 1`. Requires Screen Recording permission for the terminal app. On macOS 15 (Sequoia), ffmpeg must be built with ScreenCaptureKit support — older builds hang. The `-r 30` output flag is required because avfoundation ignores `-framerate` for screen capture. **The failure mode is silence, not an error**: ffmpeg keeps running at ~20% CPU and never writes a byte — no permission for the hosting app, or ScreenCaptureKit (`replayd`) wedged. Seen 2026-09-09: three shares in a row from Ghostty worked, then after a 28-minute share ended every new capture from the same terminal went silent, with the exact same arguments, indices and filter (all verified separately), and `replayd` cannot be restarted at all (SIP-protected: `sudo killall replayd` is a silent no-op). The wedge turned out to be **per hosting app**: `screencapture -x` still worked from the same Ghostty shell, and the very same client launched from Terminal.app captured immediately — so the discriminator is `screencapture -x /tmp/t.png` (works → not the system, not the permission), and the remedy is to quit and reopen the terminal app, or run from another one. `VideoManager` now treats 8 s without data as a failure (`SCREEN_SILENCE_MS`): kills the grabber, walks to the next candidate where there is one (which is also how an RDP session on Windows falls off ddagrab without waiting for an exit), and otherwise ends the share with the reason in the room log — including the remedy. `AVCaptureScreenInput`, which ffmpeg's avfoundation uses, is deprecated on macOS 15; the durable fix is a native ScreenCaptureKit capture (backlog).
17. **Terminal screen capture resolution**: the screen's own aspect ratio with the short side capped at 1080 and the long side at 3840, at 30 fps — `screenOutputSize(device)` in `capture-args.ts`, from the enumerated width/height: a 3440x1440 ultrawide goes out as 2580x1080, a 5120x1440 as 3840x1080, a portrait 1080x1920 unchanged, a 4K panel as 1920x1080, and nothing is ever enlarged. It used to be a fixed 1920x1080 with letterboxing, which cost an ultrawide 138 px of black above and below and 25 % of its text size. The size is still fixed *per share* and computed before ffmpeg starts, because the pipe reader must know the I420 frame size before the first byte; the filter is `scale=W:H:force_original_aspect_ratio=decrease:force_divisible_by=2,pad=W:H:…` with the computed W:H, so a grabber delivering a different shape than enumerated (mode change, RDP, Retina) is letterboxed rather than mis-sliced. Unknown size → padded 1920x1080. `scripts/screen-size-test.ts` pins the rule in CI. Higher rates (2K@60) overwhelm the event loop with raw frame data (~330 MB/s); an ultrawide at 1080 tall is a third more bytes than 1080p, within what the loop carried in the measurements.
18. **Terminal renegotiation for screen share**: `setScreenTrack()` toggles transceiver 2 direction and triggers renegotiation. The renegotiation offer SDP is munged to force `a=sendrecv` on the screen m-line because @roamhq/wrtc may not reflect direction changes. If the ffmpeg capture process dies while sharing (permission denied, no desktop, crash) `VideoManager.onScreenCaptureEnded` fires and the engine, if it still thinks it is sharing, calls `stopScreenShare()`, so the transceiver goes back to `recvonly`, peers get `isScreenSharing: false` and the room log shows why; without it the peer would keep a dead screen track.
19. **Terminal debug log file**: When `--debug` is on, all debug events are written to `~/.config/openmeet/debug.log` via `appendFileSync`. Tail with `tail -f ~/.config/openmeet/debug.log`.
20. **Audio format**: Every frame crossing the backend boundary is interleaved stereo Int16, 480 frames (10 ms). Backends upmix mono mics and downmix to mono outputs themselves. `boostOpusQuality()` sets `stereo=1;sprop-stereo=1` plus `maxaveragebitrate` from the receive setting (128 kbps by default).
21. **audify (RtAudio) quirks**: (a) its error callback is a *static* shared by every `RtAudio` instance and its destructor calls `closeStream()` unconditionally, so a garbage-collected throwaway instance emits `RtApiCore::closeStream(): no open stream to close!` — keep one long-lived probe instance and treat error types below `UNSPECIFIED` (2) as warnings. (b) `RTAUDIO_MINIMIZE_LATENCY` makes CoreAudio pick the device's *minimum* period (15 frames on common hardware → ~3000 callbacks/s); never pass it. (c) Thread-safe callbacks are only released when a stream is reopened or the object is collected, so a process with an open RtAudio stream will not exit on its own — the CLI already calls `process.exit()` after Ink unmounts; scripts must too. (d) `openStream` returns the driver's actual period; the backend re-chunks both directions to 480 frames. (e) No default-device-change notifications: a device that disappears surfaces as an error and `AudioManager` reopens (falling back to system defaults). (f) Prebuilds exist for darwin arm64/x64, linux x64/arm64, win32 x64/ia32 — no win32-arm64. (g) On Windows with *zero* audio endpoints (a CI runner, a machine with every device disabled) opening RtAudio crashes the process: `RtApiWasapi::probeDevices: No devices found.` then SIGSEGV. CI therefore only loads the module on Windows runners and enumerates on the other two; the engine process contains the crash if a user ever hits it. (h) audify ships no prebuilt binary in its npm tarball: an install script downloads it, so any environment that blocks package install scripts leaves the app without audio (`install.ps1` verifies and repairs this).
21b. **Audio bugs that only ears found (Sept 2026), and how to find the next one**: (1) *Windows → Mac sounded robotic*: the Mac's sox backend wrote a *view* of the reused playback frame to the FIFO stream; Node queues chunks by reference when the pipe write is pending, so later frames overwrote queued ones. Always copy before `stream.write()`; audify's `write()` copies synchronously so the RtAudio path is fine. (2) RtAudio's WASAPI capture resampler is audibly bad, so capture runs at the device's native rate (44.1 kHz on the Roland) and libwebrtc resamples. (3) Do not clock playback from `rec`'s pipe: it is bursty and on another device clock. Method that worked: `OPENMEET_TEST_TONE=1` on the sender, `OPENMEET_DUMP_DIR` on both ends, and on the Mac a BlackHole loopback (`--output-device "BlackHole 2ch"` + `sox -t coreaudio "BlackHole 2ch" … trim 0 15`) analysed for discontinuities; the dump of the *mix* cannot see playback-stage bugs, only the loopback can. A pure tone hides NetEQ time-stretching (WSOLA is seamless on periodic signals), so also read the `NetEQ` debug lines (accel/decel/concealed) when speech is the complaint.
21c. **Device adaptation (Sept 2026)**: `--input-channels` / `--input-gain` are persisted to settings by the TUI and travel to the engine inside `JoinOptions.input` (the engine never reads settings for audio conditioning). The RtAudio backend's `SampleQueue` re-chunks between driver periods and 10 ms frames in both directions. Continued —: the RtAudio backend opens each device at its *native* rate and channel count and converts in-process (`resampler.ts`), never via the driver: WASAPI's capture resampler is audibly bad and CoreAudio would otherwise have its nominal rate changed under the user. Duplex when input and output rates match, split streams otherwise. Mono mics upmix, mono outputs downmix, interfaces with >2 channels are listed per pair (`Name [ch 3-4]`, `AudioDevice.firstChannel`). `channels.ts` handles the "mic on input 1 only" case and gain. Verified with `scripts/audio-matrix.ts` on the Mac (15 in / 15 out incl. 96 kHz and mono devices) and the Windows VM (44.1 kHz), with `resampler-test.ts` (88–98 dB SNR) and a loopback recording of live playback on both ends.
22. **Device ids differ between backends**: sox lists macOS devices by `system_profiler` name (`MIC (BRIDGE CAST X V2-II)`), RtAudio by CoreAudio name (`Roland: MIC (BRIDGE CAST X V2-II)`). Settings persist ids by name, so switching backends shows the device picker once. RtAudio duplicates are disambiguated with ` (2)`, ` (3)`.
23. **Windows**: config/debug log live in `%APPDATA%\openmeet` (`CONFIG_DIR` in settings.ts). A `settings.json` written by Notepad or PowerShell's `Set-Content -Encoding UTF8` carries a **UTF-8 BOM**, which `JSON.parse` rejects — and `readFromDisk`'s catch then returns DEFAULTS, so every saved setting is silently lost. `readFromDisk` strips the BOM; keep it that way, and remember the same trap applies to any JSON we read on Windows (`wt-profile.cjs` hit it first). Screen sharing uses ffmpeg **`ddagrab`** (Desktop Duplication, `output_idx` per monitor, `dup_frames=false` so a still desktop costs nothing — 41% of a core down to 3%) with an automatic fallback to `gdigrab` (`-i desktop` with `-offset_x/-offset_y/-video_size`, offsets relative to the primary monitor's origin in physical pixels) when DDA has no output, and ffplay for display; webcam is gated off by `platform.ts` `webcam: false`, which `index.tsx` turns into `webcamEnabled` on `RoomState` (the `v` key, the status bar, the engine's join-time capture) while `webcamCaptureArgs()` returns `null` so `VideoManager.startCapture` refuses regardless of caller. **Screen capture needs the interactive desktop**: a process started over SSH lives in session 0 and sees a fake 1024x768 "WinDisc" display, gdigrab fails with error 5; `schtasks /create … /it` + `/run` puts a process in the user's session, which only has a display while that session is connected (console, RDP or Parsec; a disconnected session fails the same way). In an RDP session gdigrab captures the RDP client's resolution, not the physical monitor's. Test capture from the desktop shortcut, or drive the TUI from a scheduled task with `SendKeys` (see the test-rig memory). The sox backend throws on Windows (no mkfifo). The Windows VM (`ssh winvm`) is QEMU without a sound device: WASAPI enumerates zero endpoints over SSH, and only "Remote Audio" endpoints exist inside an RDP session with audio redirection.
24. **Ink 7**: `render(..., { alternateScreen: true, incrementalRendering: true })` replaces the manual `\x1b[?1049h` handling. The `\x1b[3J` strip patch on `process.stdout.write` is still applied. Ink 7 requires React 19.2 and Node 22.
24b. **The video paths are audio work.** Everything that runs on the engine's loop delays the 10 ms audio cadence, and audify silently drops a capture frame it cannot deliver (`NonBlockingCall`), so a video cost of a few ms is an audible dropout. Sharing a screen used to produce 7 to 11 dropouts every 10 s on the Windows box; the frame assembler, the 1:1 receive path and `dup_frames=false` took that to zero. Before changing anything on these paths, run `scripts/av-bench.ts` and compare against `docs/performance.md`. On Windows the engine also raises itself to `HIGH_PRIORITY_CLASS` at join (`os.setPriority`, no elevation needed): RtAudio's WASAPI thread already has MMCSS "Pro Audio", but the mixing happens on an ordinary event loop that a fullscreen game — with Game Mode deprioritizing background apps — would otherwise outrank.
25. **Audio and the TUI are separate processes — keep it that way.** Before the split, a render (10–40 ms on a 2-vCPU Windows VM) on the same loop as the 10 ms audio path caused ~7 capture gaps/s and 10% dropped playout. Now the engine loop only sees audio callbacks, signaling and a stats poll; on the same VM its p99 dropped from ~50 ms to single digits. Diagnostics with `--debug` (or `OPENMEET_LOG=1`, log file without the in-TUI feed), every 10 s in `debug.log`: `Engine loop delay`, `TUI loop delay` (both from `startLoopDelayMonitor` in `lib/diagnostics.ts`), `Ink render` (frames + ms per frame, sent from the TUI through the `log` command), `Audio clock` (input-callback gaps >30 ms). Device enumeration in the engine must stay async (`execFile`, never `execSync`): it runs next to live audio. Snapshots are quantized (VU levels to bar steps) and coalesced (≤10/s) so the TUI renders only when something visible changed. Do not add anything time-sensitive to the TUI process, and do not add heavy work to the engine's loop (RNNoise at <1 ms/frame is fine; anything blocking is not).
26. **Test rig (Sept 2026)**: a real Windows 11 machine (`ssh win`, 192.168.68.61, i7-9700K, Windows Terminal, Node 24 LTS + pnpm via winget/npm, repo copy at `C:\Users\mvega\openmeet` synced with tar over scp) with a Roland BRIDGE CAST X V2-II (WASAPI `MIC (BRIDGE CAST X V2-II)` / `Speakers (BRIDGE CAST X V2-II)`, 44.1 kHz); the Mac has the V2-I (`Roland: MIC (BRIDGE CAST X V2-I)` for RtAudio). The QEMU VM (`ssh winvm`) is the older rig. The default shell over SSH to Windows is cmd, which mangles double quotes inside the command: put anything with quotes in a `.cmd`/`.ps1` file and run that (`C:\Users\mvega\openmeet.cmd`, `openmeet-test.cmd` with the Roland devices, `matrix.cmd`, `smoke.cmd`, `win-setup.ps1`). Run the TUI over `ssh -tt win …` so Ink gets a ConPTY (pipe `sleep N |` into ssh to keep stdin open; on hang-up the engine reports exit code 3221225786 = STATUS_CONTROL_C_EXIT, that is the session closing, not a crash). `wss://openmeet.mvega.pro/ws` is not deployed and the Ubuntu host may be off: run the server on the Mac (`node packages/server/dist/index.js`) and point both clients at `ws://<mac-ip>:3001/ws`. Minimized detection needs a real window, so over SSH the watcher logs "host window not found".
27. **`setParameters` is unusable after negotiation in @roamhq/wrtc**: `getParameters()` does not expose the per-encoding `ssrc`, so the round-trip fails with `Attempted to set RtpParameters with modified SSRC` once a sender has one. Everything must be set *before* `setLocalDescription` — `sendEncodings` on `addTransceiver` (offerer) or `setParameters` right after `setRemoteDescription` (answerer, which builds senders with `addTrack` and would otherwise carry no parameters at all). Of the encoding fields only `priority`, `maxBitrate` and `degradationPreference` survive; `networkPriority` and `maxFramerate` are dropped silently and never come back from `getParameters()`. `networkPriority` would be a no-op regardless: libwebrtc leaves `enableDscp` false by default, and Windows ignores `setsockopt(IP_TOS)` without a machine-wide QoS policy. Consequence: a live connection keeps the ceiling it was created with until it renegotiates.
27b. **The screen bitrate ceiling is two levers, and only one of them moves.** `sendEncodings.maxBitrate` on the screen transceiver (from `screenSendKbps`, split by peers) is per stream but frozen at connection time — `setParameters` after negotiation is `InvalidStateError` in this binding even with the ssrc injected from the local SDP (probed 2026-09-09). The moving lever is `b=AS` on the screen m-line: in the description libwebrtc treats as *remote* it becomes the bandwidth estimator's ceiling towards that peer (probed: `availableOutgoingBitrate` followed 600 → 600, 1500 → 1382 exactly), and it is read again on every offer/answer. So `PeerConnectionManager.remoteSdp` writes the *current* send budget every time — `screenBitrateFor(peers, setting) × pixelFactor(shape) + 384 kbps for audio` — and `PeerConnectionManager.refreshBudgets()` re-offers the *other* peers whenever the number of connections changes during a share — on `removeConnection`, on a `failed` connection, and for a newcomer only once its connection is `connected`, so the re-offer never collides with that peer's own first offer (a room event would fire before the newcomer even has a connection). That is what closed the backlog's "`screenBitrateFor` reads like a policy but behaves per-peer". Our own description carries `screenReceiveKbps + 384` the same way: what we ask each peer not to exceed towards us. Caveats: it is a ceiling for the whole call to that peer (hence the allowance), it bounds what the encoder is *offered* rather than each frame (VP8 screenshare overshoots on pathological content either way), and a browser-grade hard clamp would need `setParameters`, which this wrtc has not got. `scripts/share-probe.ts` asserts the budget line; `scripts/sdp-test.ts` the SDP surgery.
28. **ddagrab needs a real GPU output, and no GPU scaler works**: inside an RDP session DDA fails with `Failed to enumerate DXGI output 0` while gdigrab captures that session fine, hence the fallback in `capture-args.ts`. Keeping the frame on the GPU is not an option either — measured on ffmpeg 9.0.1 (Gyan full build, RTX 2080 SUPER): `scale_d3d11` fails with `Could not create the texture (80070057)`, and `hwmap=derive_device` to both CUDA and Vulkan returns `-40` (ENOSYS). So the colour conversion is downloaded and done on the CPU. `h264_nvenc` *does* accept ddagrab's D3D11 frames directly at 1% of a core, but wrtc offers no H.264 and has no hardware encoder factory, so that only pays off if video ever leaves WebRTC. `output_idx` is taken from the `Screen.AllScreens` enumeration order, which matches DXGI on a single-adapter machine but is unverified with several monitors or GPUs. Never put `-r` on the output: it restores the frame duplication `dup_frames=false` just avoided.
29. **`@jitsi/rnnoise-wasm` must be imported by full path**: its package entry re-exports with extensionless specifiers that Node's ESM resolver rejects (`ERR_MODULE_NOT_FOUND` at runtime, though bundlers and tsx resolve it), so `noise-suppression.ts` imports `@jitsi/rnnoise-wasm/dist/rnnoise-sync.js` directly. The `@shiguredo` build is browser-only and throws "not compiled for this environment" under Node.
30g. **Only the offerer can set send parameters in @roamhq/wrtc.** The answerer's senders come from `addTrack` and `setRemoteDescription`; before negotiation `getParameters()` reports `encodings: []` and adding one is refused with "Attempted to set RtpParameters with different encoding count", and after negotiation the ssrc check refuses everything (gotcha 27). So an answerer's audio keeps the allocator's default share and its screen share is uncapped until it becomes the offerer of a renegotiation. `applySendParameters` therefore never changes the encoding count, and it logs refusals instead of swallowing them — a silent catch there hid this for a while.
30f. **`incrementalRendering` is off, and a resize is why.** Ink's incremental writer positions the cursor from the previous frame's geometry; a resize invalidates that, because the terminal reflows the rows already on screen, so the walk lands in the wrong place and rows keep their old right edge — the frame's right border vanished on some rows and not others. Dragging a window edge corrupted it 3 times out of 4; with full frames it survived every resize tried (width-only, height-only, one-column steps, large jumps), verified by reading Ghostty's own text buffer through the accessibility API after each one. The old note claiming a full-frame ConPTY write blocks 50-70 ms does not reproduce: measured on the real Windows box it is p50 0.19 ms, and a full repaint costs 117 KB/s against 12 KB/s. Also: verify a render fix after **every** step, not once at the end — sampling only the final state is how the first attempt at this was called fixed when it was not.
30e. **Never set the frame's `width` from our own `resize` listener.** Ink already lays the root out to the terminal width; `app.tsx`'s `FullScreen` used to mirror `process.stdout.columns` into React state and pass it as `width`, which made the terminal size two sources of truth updated at different times. Dragging a window edge fires dozens of resize events, Ink writes a frame on each one from the tree as currently committed, and a frame would go out at the previous width while the terminal was already wider — with incremental rendering the stale rows then stayed on screen. That was the missing right border: it survived only on rows left over from a frame written at the right width. A Box with no `width` stretches to the terminal on its own; only `height` needs tracking, and only so the painted background reaches the bottom. Verified by tapping `process.stdout.write`: Ink's bytes were always correct — every line closed with its border — so this is ours, not Ink's.
30d. **Ink wraps a Text that does not fit; it does not clip it.** Section rules used to be `<Text>{'─'.repeat(200)}</Text>` inside a `<Box height={1} overflow="hidden">`. The 200 columns are 200 columns whatever the container is, so the excess ran over the frame's right border and off the row — the right edge of the window disappeared on some rows and not others, and resizing changed which. `overflow="hidden"` on an ancestor does not save you: the Text wraps first. Use `Rule` from `components/text.tsx` (a Box with only a top border, laid out by Yoga) and never a repeat-count that assumes a width. The same trap applies to any padded or centred string built with `repeat`.
30c. **A Box's `backgroundColor` does not reach its border.** Ink draws borders in a separate pass (`render-border.js`), so a framed box paints its interior but leaves the frame's own cells showing whatever is behind them — the terminal's background, which is the one thing the theme is trying not to depend on. Set `borderBackgroundColor` alongside `backgroundColor` on every box that has a border.
30b. **Never use a named colour or `dimColor` in the TUI.** Both are resolved by the terminal's theme, which is why the same screen looked different in every terminal. Import `theme` from `lib/theme.ts` and `Text` from `components/text.tsx` — the wrapper defaults the foreground and remaps `dimColor` — and the app paints its own background in `app.tsx`, so it renders identically on a light terminal too, as a dark panel. `ink-select-input` is wrapped for the same reason (`components/select.tsx`: the stock items used `blue` and no foreground) and `ink-text-input` was replaced outright (`components/text-input.tsx`), and `biome.json` enforces all three — `Text` from `ink`, `ink-select-input` and `ink-text-input` are `noRestrictedImports` everywhere but the wrapper. `scripts/` and non-UI code are unaffected.
30h. **NVIDIA Broadcast is detected, not integrated — and that is the whole point.** Its virtual mic enumerates as a plain WASAPI endpoint, so there is no SDK, no API and nothing to link; `audio/nvidia-broadcast.ts` only recognises the name so the UI can recommend it and the engine can *skip RNNoise* while it is the input (denoising denoised audio costs 0.22 ms on the one loop that cannot afford it). Two traps: (a) the machine may have other NVIDIA audio endpoints that are **not** Broadcast — the real test box lists `NVIDIA High Definition Audio` and `NVIDIA Virtual Audio Device (Wave Extensible) (WDM)` — so match `/nvidia broadcast/i`, never `/nvidia/i`; a false positive silently turns off noise suppression someone asked for. (b) A selection of `undefined` means "system default" and carries no name, so a user whose Windows default *is* Broadcast would look like a plain mic; `resolveBroadcastDefault` names it in `app.tsx`, which opens the identical endpoint and gives up only following a later default change — which we never followed anyway (gotcha 21e). The presence of the device is also the RTX check, because Broadcast refuses to install below an RTX 20-series; the `hasRtxGpu()` PowerShell probe exists only for the "you have an RTX, you could install this" hint. `scripts/nvidia-broadcast-test.ts` covers the matching in CI.
30. **Noise suppression (done; echo cancellation deferred on CPU, available on GPU)**: RNNoise runs as a `CaptureProcessor` (`audio/noise-suppression.ts`), attached by the engine at join when the `noiseSuppression` setting is on, off by default, and skipped entirely when the input is the NVIDIA Broadcast mic.
31. **Explorer's PATH is stale, so never `spawn('ffmpeg')` by name.** Explorer captures the environment when it starts and never re-reads it; a desktop shortcut inherits that copy. On the Windows test box a window opened from the shortcut had `Roaming\npm` on its PATH but not `%LOCALAPPDATA%\Microsoft\WinGet\Links` — where `winget install Gyan.FFmpeg` (what `install.ps1` runs) puts ffmpeg — while `where ffmpeg` succeeded over SSH. The app then silently disabled video: `s` did nothing and `e` never appeared, and the stderr warning was hidden under the alternate screen. `lib/tool-path.ts` resolves the tools itself and the engine logs `Video tools: <paths>` at join; when video is off the room log says why (`videoDisabledReason`).
32b. **A decoded frame's header can outrun its data; check the length before copying.** `RTCVideoSink.onframe` gives `{width, height, data}`, and around a resolution change — the sender switching camera or mode — the geometry can move on while the buffer has not. `video.ts` used to copy `i420FrameBytes(width, height)` from `data.buffer`, which past the end of a short frame reads whatever the allocator has next: the picture comes out repeated horizontally and combed, which reads like a codec fault and is not one. Seen 2026-09-09 on the Windows client watching a Mac camera that had just gone from 640x480 to 1280x720; the capture, the camera's own modes and two simultaneous openers were all verified clean first. The frame is now skipped when `data.byteLength` falls short, logged once per peer.
32. **Diagnosing "the video window never opened": the header's `↑/↓` are audio only.** `Video in from <peer> mid N` / `Video out to <peer> mid N` lines in `debug.log` (every 10 s with `--debug` or `OPENMEET_LOG=1`) carry kbps, fps, geometry, packets, frames decoded/encoded, keyframes, PLI and `qualityLimitationReason`. `ffplay` is spawned on the **first decoded frame** (`VideoManager.wireSink`), so `Video peer added` without `ffplay started` means the decoder delivered nothing — and the stats say whether that is "no packets" (sender) or "packets but no decode" (receiver/keyframe). Verified 2026-09-09: a Windows screen share received on a fresh connection decodes at ~18 fps / 200 kbps and stays decoded when we stop our own share; the mid-call case that showed `packets 0` was gotcha 33 — the sender side, found by these very lines on both ends. `targetBitrate` is not exposed by this wrtc (always 0); `BWE to <peer>` (`availableOutgoingBitrate`) is.
34. **A chat entry is one `Text`; a row `Box` of several is not sized for wrapping.** The old chat put each line in a `Box` holding the timestamp, the name and the message as separate `Text`s. A message longer than the width wrapped to two rows in the output, but Yoga kept the row's one-row height, so the second row was painted over the next entry and rows went missing (`scripts/chat-test.ts` reproduced it from the frames). With the pieces nested inside a single `Text`, Ink measures the wrapped height itself, and a `justifyContent="flex-end"` box with `overflowY="hidden"` then does the rest: the newest entries sit at the bottom and whatever runs past the top is clipped — including the head of a long entry, which shows its tail. The first fix measured every entry with `wrap-ansi` and rendered only whole ones; it was the row `Box` that was wrong, not the clipping, so that machinery went. Hand Ink at most as many entries as the terminal has rows: each takes at least one, so nothing more can be visible.
33. **`getParameters()` lies on the Windows prebuild of @roamhq/wrtc — never write its read-back straight into `setParameters()`.** Right after `addTransceiver(…, { sendEncodings })`, `sender.getParameters().encodings[0].maxBitrate` on Windows is uninitialised memory: a denormal such as `4.3e-312` some of the time, the real value other times (macOS always reads correctly). `applySendParameters` used to round-trip that object to set `degradationPreference`, and the garbage landed as a **0 bps ceiling** — the screen encoder then never produced a frame (`Video out … mid 2: encoded 0, limited by none`, SDP flawless at `sendrecv/sendonly`). Only offerer-created senders take that path, which is why a share started mid-call from Windows was invisible while the same machine answering a newcomer with the track already attached worked. `priority` never comes back at all, on any platform. The fix merges the intended encoding over the read-back (`{ ...read, ...encoding, active: true }`). `scripts/share-probe.ts` runs the app's own `PeerConnectionManager` back to back in one process — no devices — and fails if a screen sender encodes nothing; it runs in CI on all three OSes, so a regression here cannot be silent again. Field notes on the hunt are in gotcha 32 and the 2026-09-09 backlog entry it replaced.
35. **Updating ourselves: npm hides a prefix it was given, and skips the install script that matters.** Two npm behaviours shape `lib/update.ts`. (a) `npm root -g` prints `***` where the prefix should be whenever that prefix came from the environment — npm treats `prefix` as a protected option, and `npm config get prefix` refuses outright ("the prefix option is protected") — so comparing its answer with our own directory failed for exactly the people who set `npm_config_prefix`, which is the usual way to escape a root-owned global directory. Measured 2026-09-10 against a sandbox prefix. What decides now is `process.argv[1]`: the package root is the path up to `node_modules/openmeet-terminal`, a package.json two levels above it means a local dependency rather than a global install, and the real proof comes after the install — the package on disk must report the new version, or npm replaced a different copy. (b) `npm install -g` runs no package install scripts unless they are allowed by name, and audify's is what fetches its binary (gotcha 21h), so a fresh install can succeed and leave the app mute; `applyUpdate` retries once with `--allow-scripts=audify` — the repair `install.ps1` already does — and rolls back to the version that was working if the modules still do not load. And the install can only run *after* Ink unmounts and the engine exits: that process holds both `.node` files, and Windows will not let npm replace a directory in use.
36. **A published bundle runs React's *development* build unless you make it not.** React picks its build from `process.env.NODE_ENV` when it is first imported. Nothing in the app set it, and `packages: 'external'` meant React was resolved from the user's `node_modules` at run time — so every install ran the development reconciler, with all of its checks, for the whole life of the call. Measured on this Mac (docs/performance.md, Sept 2026): **31 ms of CPU per full room render against 18 ms**, and ~28 MB more resident; four clients in one room, two per build, showed 2.5–3.3% against 0.7–0.8% of a core. Two traps in fixing it: setting `process.env.NODE_ENV` at the top of the entry point does nothing, because ESM hoists every static import above it — the React that reads the variable has already loaded; and esbuild's `define` cannot reach a package left external, since the replacement happens in *our* code, not in the copy npm installed. So React and Ink are bundled and `process.env.NODE_ENV` is defined at build time, which also dead-code-eliminates the development branches and takes `es-toolkit`'s 12 MB out of the install. The `dev` script still runs the development build through tsx, which is where the warnings are worth having.
37. **The TUI's cost is how often something changes, not how much is on screen.** Ink re-renders the whole tree and reserialises the whole frame for any state change — `incrementalRendering` is off (gotcha 30f) — so a frame costs the same whether one cell moved or all of them. The room used to show a 10-cell VU meter per participant, quantized to 80 steps and refreshed every 100 ms, which meant ~8.5 full-screen repaints a second with nobody doing anything: 90–100 KB/s of ANSI out of the process, ~10% of a core in it, and the same again in the terminal emulator parsing and painting it. The meter is gone from the room (gotcha 38); what is left changes a couple of times a second. Before adding anything that updates continuously — a clock with seconds, a meter, a spinner, a live counter — price it at one full repaint per update, and put it behind a change that a person would notice.
38. **The speaking dot is the voice gate, and the voice gate is the send path.** `audio/voice-gate.ts` decides whether capture frames reach `RTCAudioSource` at all: while it is shut nothing is encoded here, sent to anyone, or decoded anywhere in the room, and a muted participant costs every peer exactly nothing (mute holds the gate shut *without* feeding it, so a long mute does not teach it that the room has no noise floor). The dot beside each name is that state — ours from the gate directly, a peer's from the audio they send — which is why it changes when someone starts or stops talking and not ten times a second. Measured: a silent room of four went from 17.1–17.7% of a core per engine to 6.1–6.4%. What remains when nobody speaks is the receive side — libwebrtc's fake audio device module pulls playout every 10 ms per connection whether or not a packet arrived (gotcha 35b) — and that one is not ours to turn off.
35b. **`@roamhq/wrtc` decodes and mixes on a `TestAudioDeviceModuleImpl` thread whether you use it or not.** `sample` on a process doing nothing but sending one audio track shows that thread running `AudioTransportImpl::NeedMorePlayData` → `AudioMixerImpl::Mix` → NetEq → `opus_decode` every 10 ms. It is not waste to remove: that pull is what feeds `RTCAudioSink`, which is how we get a peer's audio at all. It is why a receiving connection has a floor cost, why that floor multiplies by the number of peers in the mesh, and part of why one PeerConnection carrying our audio measures ~7% of a core when the Opus encoder inside it measures 0.66% (docs/performance.md).
